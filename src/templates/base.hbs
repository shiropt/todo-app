import styled from "styled-components";
import { FC } from "react";


type Props = {
  className?: string;
}

export const Component:FC<Props> = ({className}) => {
  return (
    < className={className}>
    </>
  );
};

export const {{properCase name}} = styled(Component)``;

